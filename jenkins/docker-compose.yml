services:
  jenkins:
    image: jenkins/jenkins:lts

    container_name: jenkins

    user: "1000:1000"

    ports:
      - "8080:8080"

    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/flask-sql-docker:/app

volumes:
  jenkins_home: